# Calculator Hub

A Nuxt.js app — a collection of calculators.

## Quick Start

```bash
npm install
npm run dev
# -> http://localhost:3000
```

## Other Commands

```bash
npm run build      # production build
npm run preview    # preview production build locally
npm run generate   # static site generation
```
